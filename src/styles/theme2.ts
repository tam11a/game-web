import { createTheme } from "@mui/material";

export const primaryGradient: string = 'linear-gradient(180deg, #0074FD 0%, #00D1FF 100%)'

const theme = createTheme({
    typography: {
        fontFamily: 'Raleway, sans-serif'
    }
});

export default theme;